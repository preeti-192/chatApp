{
  "name": "nodeserver",
  "version": "1.0.0",
  "description": "This is node server for chat app",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Preeti",
  "license": "ISC",
  "dependencies": {
    "socket.io": "^4.8.0"
  }
}
